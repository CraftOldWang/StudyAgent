# 阶段 0.1：引入 AgentScope 依赖

- 对应任务：[GitHub Issue #1](https://github.com/CraftOldWang/StudyAgent/issues/1)
- Scope：仅建立 AgentScope 2.0.1 的 Maven 依赖基线。

## 实际变更

- 新增统一版本属性 `agentscope.version=2.0.1`。
- 新增 `agentscope-harness`、`agentscope-extensions-model-dashscope`、`agentscope-extensions-model-openai` 三个直接依赖。

## 验证

```powershell
mvn dependency:tree "-Dincludes=io.agentscope"
mvn compile
mvn test
```

- `dependency:tree`：退出码 0；三个直接依赖均解析为 2.0.1，`agentscope-harness` 传递引入 `agentscope-core` 2.0.1。
- `compile`：退出码 0，构建成功。
- `test`：退出码 0；共运行 53 个测试，失败 0、错误 0、跳过 2。

## 独立验收

- 状态：PASS。
- 验收角色：独立验收 subagent（非本任务实现者）。
- 命令证据：`dependency:tree`、`compile`、`test`、`git diff --check` 均通过；测试共 53 个，失败 0、错误 0、跳过 2。
- Git 状态：当前变更仍未 commit、未 push。
